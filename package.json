{
  "name": "llm-tops-calculator",
  "version": "1.0.0",
  "description": "Interactive calculator that estimates how many TOPS you need to run local LLMs at different quantizations.",
  "private": true,
  "scripts": {
    "test": "bun test tests/calculator.test.js",
    "test:e2e": "bunx playwright test",
    "test:all": "bun run test && bun run test:e2e",
    "serve": "bunx http-server -p 8000 -c-1",
    "build:og": "bun scripts/render-svg.mjs og-image.svg og-image.png 1200 630",
    "build:infographic": "bun scripts/render-svg.mjs docs/infographic.svg docs/infographic.png 1200 1200",
    "build:images": "bun run build:og && bun run build:infographic"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "1.55",
    "http-server": "^14.1.1"
  }
}
