{
  "name": "llm-tops-calculator",
  "version": "1.0.0",
  "description": "Interactive calculator that estimates how many TOPS you need to run local LLMs at different quantizations.",
  "private": true,
  "scripts": {
    "test": "node --test tests/calculator.test.js",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e",
    "serve": "http-server -p 8000 -c-1"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "1.55",
    "http-server": "^14.1.1"
  }
}
